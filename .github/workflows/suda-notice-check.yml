name: SUDA Notice Check (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled-noop:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy scheduled email disabled
        run: |
          echo "Legacy SUDA scheduled email sending is disabled."
