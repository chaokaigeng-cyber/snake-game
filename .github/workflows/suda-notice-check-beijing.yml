name: SUDA Notice Check Beijing Time (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled-noop:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled email disabled
        run: |
          echo "SUDA scheduled email sending is disabled."
